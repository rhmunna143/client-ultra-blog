// this is the login page
import LoginForm from '@/components/Login/LoginForm';
import React from 'react';

const page = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default page;